import { Target, TargetFormat } from '../../interfaces/Output';

export class logger  {
    constructor() {};
    
    /**
     * name
     */
    public createConnectionString() : Target {
        
        return ;
    }
}